spring:
  application:
    name: domino

firebase:
  project-id: ${FIREBASE_PROJECT_ID:teamfho-domino}

server:
  port: ${PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
      show-components: never
